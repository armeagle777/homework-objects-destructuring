let person = { name: 'Sarah', country: 'Nigeria', job: 'Developer' };
// let name, country, job;
const { name, country, job } = person;

//? Testing -> declaration of variables name, country and job with let keyword must be removed
console.log(name);
